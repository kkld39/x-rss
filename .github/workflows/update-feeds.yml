name: Update X RSS

on:
  schedule:
    - cron: '37 * * * *'
  workflow_dispatch:

concurrency:
  group: x-rss-pages
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  update:
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Check application
        run: python -m unittest discover -s tests -v
      - name: Fetch X and build RSS
        id: update
        continue-on-error: true
        run: |
          owner="${GITHUB_REPOSITORY_OWNER,,}"
          repo="${GITHUB_REPOSITORY#*/}"
          SITE_URL="https://$owner.github.io"
          if [[ "${repo,,}" != "$owner.github.io" ]]; then
            SITE_URL="$SITE_URL/$repo"
          fi
          export SITE_URL
          python -m xrss.app
      - name: Save history and generated feeds
        if: steps.update.outputs.ready == 'true'
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- data public
          if ! git diff --cached --quiet; then
            git commit -m 'Update X RSS history and feeds [skip ci]'
            git push origin "HEAD:$DEFAULT_BRANCH"
          fi
      - name: Read Pages settings
        if: success() && steps.update.outputs.ready == 'true'
        id: pages
        uses: actions/configure-pages@v5
      - name: Package site
        if: success() && steps.update.outputs.ready == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: public
          retention-days: 1
      - name: Deploy Pages
        if: success() && steps.update.outputs.ready == 'true'
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Fail workflow only for actionable application errors
        if: always() && steps.update.outcome == 'failure'
        run: |
          echo 'Application, saved data or RSS generation needs attention. Temporary errors with valid cached RSS are warnings.'
          exit 1
